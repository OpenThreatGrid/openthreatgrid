apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: openthreatgrid
  labels:
    app: postgres
spec:
  clusterIP: None  # headless, for the StatefulSet
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
