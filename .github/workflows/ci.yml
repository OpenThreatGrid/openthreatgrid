name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: otg-api (lint + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend/otg-api
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements-dev.txt
      - name: Ruff
        run: ruff check .
      - name: Pytest
        run: pytest

  worker:
    name: otg-worker (lint + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: workers/otg-worker
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements-dev.txt
      - name: Ruff
        run: ruff check .
      - name: Pytest
        run: pytest

  reports:
    name: reports (render example)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: reports
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Generate example report from sample events
        run: python generate_report.py --from-file ../examples/sample-events/otg-events.json --output /tmp/report.md
      - name: Report is non-empty
        run: test -s /tmp/report.md

  manifests:
    name: helm + manifests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
      - name: Helm lint
        run: helm lint deploy/helm/openthreatgrid
      - name: Helm template
        run: helm template otg deploy/helm/openthreatgrid > /dev/null
